import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App basic initialization test', (WidgetTester tester) async {
    // Basic dummy test because the default counter app was replaced.
    expect(true, true);
  });
}
